{
  "name": "bidalert-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../bid2alert-nextjs && npm install",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd bid2alert-nextjs && npm run build",
    "build:backend": "echo Building backend...",
    "dist": "build-for-server.bat"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}