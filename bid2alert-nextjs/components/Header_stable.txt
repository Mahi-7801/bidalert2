    // ... rest of the interfaces ...

    // Header JSX modification around action section
    // (Line numbers will change but logic remains same)

    interface NavColumn {
        title: string;
        links: NavLink[];
    }

    interface NavItem {
        title: string;
        links?: NavLink[];
        columns?: NavColumn[];
    }

    const navItems: NavItem[] = [
        {
            title: 'India Tenders',
            links: [
                { label: 'View All Indian Tenders', href: '/tenders' },
                { label: 'News Paper Tenders', href: '/tenders?source=Newspaper' },
                { label: 'Archive Tenders', href: '/tenders?status=archive' },
                { label: 'Empanelment', href: '/tenders?q=Empanelment' },
                { label: 'EOI – Tenders', href: '/tenders?q=EOI' },
                { label: 'RFP – Tenders', href: '/tenders?q=RFP' },
                { label: 'RFQ – Tenders', href: '/tenders?q=RFQ' },
                { label: 'TOR – Tenders', href: '/tenders?q=TOR' },
                { label: 'Other Tenders', href: '/tenders?q=Other' },
                { label: 'Sample Documents', href: '/samples', highlight: true },
            ]
        },
        {
            title: 'Tenders By',
            links: [
                { label: 'State', href: '/tender-states' },
                { label: 'Town/City', href: '/tender-cities' },
                { label: 'Category', href: '/tender-categories' },
                { label: 'Sector/Industry', href: '/tender-industries' },
                { label: 'Department', href: '/tenders?group=Department' },
                { label: 'Bank', href: '/tender-banks' },
                { label: 'Keyword', href: '/tenders' },
                { label: 'IREPS', href: '/tenders?portal=ireps', hasArrow: true },
                { label: 'Expired/Closed/Archive', href: '/tenders?status=archive' },
            ]
        },
        {
            title: 'Global Tenders',
            links: [
                { label: 'View All Tenders', href: '/global-tenders' },
                { label: 'Country', href: '/global-tenders?by=country' },
                { label: 'City', href: '/global-tenders?by=city' },
                { label: 'Authority', href: '/global-tenders?by=authority' },
                { label: 'MAF', href: '/global-tenders?by=maf' },
                { label: 'Category', href: '/global-tenders?by=category' },
            ]
        },
        {
            title: 'IPR Services',
            links: [
                { label: 'TRADEMARK REGISTRATION', href: '/registrations/trademark' },
                { label: 'COPYRIGHT REGISTRATION', href: '/registrations/copyright' },
                { label: 'PATENT REGISTRATION', href: '/registrations/patent' },
                { label: 'DESIGN REGISTRATION', href: '/registrations/design' },
                { label: 'GEOGRAPHICAL INDICATIONS (GI)', href: '/registrations/gi' },
                { label: '', href: '#', isDivider: true },
                {
                    label: 'REGISTRATIONS',
                    href: '#',
                    hasArrow: true,
                    subLinks: [
                        { label: 'GST REGISTRATION', href: '/registrations/gst' },
                        { label: 'STARTUP INDIA REGISTRATION', href: '/registrations/startup-india' },
                        { label: 'ISO REGISTRATION', href: '/registrations/iso' },
                        { label: 'IMPORT EXPORT CODE (IEC)', href: '/registrations/iec' },
                        { label: 'PF & ESI REGISTRATION', href: '/registrations/pf-esi' },
                        { label: 'FOOD LICENSE (FSSAI)', href: '/registrations/food-license' },
                        { label: 'MSME UDYOG AADHAAR', href: '/registrations/msme' },
                        { label: '12A & 80G REGISTRATION', href: '/registrations/12a-80g' },
                        { label: 'DOT OSP LICENSE', href: '/registrations/dot-osp' },
                    ]
                },
            ]
        },
        {
            title: 'IT Services',
            columns: [
                {
                    title: 'Website Development',
                    links: [
                        { label: 'Static Website Development', href: '/solutions/web-app' },
                        { label: 'Dynamic Website Development', href: '/solutions/web-app' },
                        { label: 'E-commerce Website Development', href: '/solutions/web-app' },
                        { label: 'CRM Website or Portal Development', href: '/solutions/web-app' },
                    ]
                },
                {
                    title: 'Mobile Solutions',
                    links: [
                        { label: 'Mobile Application Development', href: '/solutions/web-app' },
                        { label: 'Custom Software Development', href: '/solutions/web-app' },
                    ]
                },
                {
                    title: 'Other Services',
                    links: [
                        { label: 'Multimedia Solutions', href: '/solutions/multimedia' },
                        { label: 'Hosting & Domain Services', href: '/solutions/web-app' },
                        { label: 'Website / App AMC & Support Packages', href: '/solutions/web-app' },
                    ]
                }
            ]
        }
    ];

    return (
        <>
            <header
                className="fixed top-0 left-0 right-0 z-[100] w-full"
            >
                <div className="w-full mx-auto transition-all duration-500 ease-in-out">
                    <div
                        className={`flex flex-col pointer-events-auto transition-all duration-500 bg-[#0A0F1C] border-b border-white/10 w-full px-0 py-0 shadow-lg`}
                    >
                        {/* Top Row: Logo & Utility Actions */}
                        <div className={`flex items-center justify-between w-full px-6 lg:px-12 py-3 transition-colors ${!scrolled ? 'bg-white/[0.01]' : ''}`}>
                            {/* Logo */}
                            <Link href="/" className="relative shrink-0 transition-transform active:scale-95 duration-200">
                                <div className={`relative transition-all duration-500 ${scrolled ? 'h-6 w-[120px]' : 'h-8 w-[150px]'}`}>
                                    <Image
                                        src="/bidalert_logo.png"
                                        alt="BidAlert"
                                        fill
                                        className="object-contain object-left"
                                        priority
                                    />
                                </div>
                            </Link>

                            {/* Action Section */}
                            <div className="hidden lg:flex items-center gap-4">
                                <Link href="/plans" className="px-2 py-1.5 text-[11px] font-bold text-white/40 border-b border-transparent hover:border-bid-green hover:text-white transition-all tracking-wider uppercase">Plans</Link>

                                {isAuthenticated ? (
                                    <>
                                        {user?.role === 'admin' && (
                                            <>
                                                <div className="h-3 w-px bg-white/10 mx-1" />
                                                <Link href="/admin/messages" className="px-2 py-1.5 text-[11px] font-bold text-white/40 border-b border-transparent hover:border-bid-green hover:text-white transition-all tracking-wider uppercase">User Requests</Link>
                                            </>
                                        )}
                                        <div className="h-3 w-px bg-white/10 mx-1" />
                                        <Link href="/my-requests" className="px-2 py-1.5 text-[11px] font-bold text-white/40 border-b border-transparent hover:border-bid-green hover:text-white transition-all tracking-wider uppercase">My Requests</Link>
                                    </>
                                ) : (
                                    <>
                                        <div className="h-3 w-px bg-white/10 mx-1" />
                                        <button onClick={openLogin} className="px-2 py-1.5 text-[11px] font-bold text-white/40 border-b border-transparent hover:border-bid-green hover:text-white transition-all tracking-wider uppercase">Admin Login</button>
                                        <div className="h-3 w-px bg-white/10 mx-1" />
                                        <button onClick={openLogin} className="px-2 py-1.5 text-[11px] font-bold text-white/40 border-b border-transparent hover:border-bid-green hover:text-white transition-all tracking-wider uppercase">My Requests</button>
                                    </>
                                )}

                                <div className="h-3 w-px bg-white/10 mx-1" />

                                {isAuthenticated ? (
                                    <div className="flex items-center gap-3">
                                        {/* Unified Notification Center */}
                                        <div className="relative group">
                                            <button
                                                onClick={() => setShowUserNotifications(!showUserNotifications)}
                                                className={`p-2 rounded-full transition-all duration-300 ${showUserNotifications ? 'bg-bid-green text-bid-dark' : 'text-white/40 hover:text-white hover:bg-white/5'}`}
                                            >
                                                <Bell size={18} />
                                                {(user?.role === 'admin' ? notificationCount : unreadUserCount) > 0 && (
                                                    <span className="absolute top-1.5 right-1.5 bg-red-500 text-white text-[8px] font-black w-3.5 h-3.5 flex items-center justify-center rounded-full animate-pulse shadow-lg shadow-red-500/50">
                                                        {user?.role === 'admin' ? notificationCount : unreadUserCount}
                                                    </span>
                                                )}
                                            </button>

                                            <AnimatePresence>
                                                {showUserNotifications && (
                                                    <motion.div
                                                        // @ts-ignore
                                                        initial={{ opacity: 0, y: 10, scale: 0.95 }}
                                                        animate={{ opacity: 1, y: 0, scale: 1 }}
                                                        exit={{ opacity: 0, y: 10, scale: 0.95 }}
                                                        className="absolute right-0 mt-3 w-80 bg-[#1e293b] border border-white/10 rounded-2xl shadow-2xl z-[120] overflow-hidden"
                                                    >
                                                        <div className="p-4 border-b border-white/5 flex justify-between items-center bg-white/5">
                                                            <h3 className="text-[10px] font-black uppercase tracking-widest text-white/40">Notifications</h3>
                                                            {unreadUserCount > 0 && (
                                                                <button
                                                                    onClick={() => markAsRead()}
                                                                    className="text-[9px] font-bold text-bid-green hover:underline uppercase"
                                                                >
                                                                    Mark all read
                                                                </button>
                                                            )}
                                                        </div>
                                                        <div className="max-h-[400px] overflow-y-auto custom-scrollbar">
                                                            {userNotifications.length === 0 ? (
                                                                <div className="p-10 text-center">
                                                                    <Bell size={24} className="mx-auto mb-3 text-white/10" />
                                                                    <p className="text-[10px] font-bold text-white/20 uppercase tracking-widest">No new alerts</p>
                                                                </div>
                                                            ) : (
                                                                userNotifications.map((note) => (
                                                                    <div
                                                                        key={note.id}
                                                                        onClick={() => markAsRead(note.id)}
                                                                        className={`p-4 border-b border-white/5 transition-colors cursor-pointer hover:bg-white/[0.02] ${!note.is_read ? 'bg-bid-green/[0.02]' : 'opacity-60'}`}
                                                                    >
                                                                        <div className="flex gap-3">
                                                                            <div className={`mt-1 shrink-0 w-2 h-2 rounded-full ${!note.is_read ? 'bg-bid-green' : 'bg-white/10'}`} />
                                                                            <div className="flex-1">
                                                                                <h4 className={`text-xs font-bold leading-tight ${!note.is_read ? 'text-white' : 'text-white/60'}`}>{note.title}</h4>
                                                                                <p className="text-[11px] text-white/40 mt-1 line-clamp-2 leading-relaxed">{note.message}</p>
                                                                                <span className="text-[8px] font-black text-white/20 uppercase mt-2 block tracking-tighter">
                                                                                    {new Date(note.created_at).toLocaleString()}
                                                                                </span>
                                                                            </div>
                                                                        </div>
                                                                    </div>
                                                                ))
                                                            )}
                                                        </div>
                                                        {user?.role === 'admin' && notificationCount > 0 && (
                                                            <Link
                                                                href="/admin/messages"
                                                                onClick={() => setShowUserNotifications(false)}
                                                                className="p-3 bg-bid-green/10 block text-center border-t border-bid-green/20 group"
                                                            >
                                                                <span className="text-[9px] font-black text-bid-green uppercase tracking-[0.2em] group-hover:brightness-125 transition-all">
                                                                    View {notificationCount} New Requests →
                                                                </span>
                                                            </Link>
                                                        )}
                                                    </motion.div>
                                                )}
                                            </AnimatePresence>
                                        </div>

                                        <div className="flex items-center gap-2 bg-white/5 pl-2.5 pr-1 py-1 rounded-full border border-white/10 hover:bg-white/10 transition-colors">
                                            <span className="text-[10px] font-black uppercase text-bid-green flex items-center justify-center w-5 h-5 bg-bid-green/10 rounded-full">{user?.name[0]}</span>
                                            <button onClick={logout} className="p-1 px-2 text-white/30 hover:text-red-400 text-[9px] font-black border-l border-white/10 ml-0.5 tracking-widest uppercase">
                                                OUT
                                            </button>
                                        </div>
                                    </div>
                                ) : (
                                    <div className="flex items-center gap-2">
                                        <button
                                            onClick={openLogin}
                                            className="px-3 py-1.5 text-[12px] font-bold text-white/60 hover:text-white transition-colors uppercase tracking-widest"
                                        >
                                            Sign In
                                        </button>
                                        <button
                                            onClick={openRegister}
                                            className="px-5 py-2 text-[11px] font-black tracking-[0.15em] bg-bid-green text-bid-dark rounded-full hover:brightness-110 transition-all uppercase active:scale-95 shadow-lg shadow-bid-green/10"
                                        >
                                            Join Now
                                        </button>
                                    </div>
                                )}
                            </div>

                            {/* Mobile Menu Btn */}
                            <button
                                onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
                                className="lg:hidden p-2 rounded-xl text-white/60"
                            >
                                {mobileMenuOpen ? <X size={24} /> : <Menu size={24} />}
                            </button>
                        </div>

                        {/* Bottom Row: Navigation Links (Primary Tier) */}
                        <div className={`hidden lg:flex items-center justify-center w-full border-t border-white/10 transition-all duration-500 ${scrolled ? 'py-1.5' : 'py-3'}`}>
                            <nav className="flex items-center gap-1 xl:gap-2">
                                <Link href="/" className="px-4 py-1.5 text-[12px] font-bold text-white/90 hover:text-white hover:bg-white/5 transition-all rounded-full tracking-wide">
                                    Home
                                </Link>
                                {navItems.map((item) => (
                                    <div
                                        key={item.title}
                                        className="relative"
                                        onMouseEnter={() => setActiveDropdown(item.title)}
                                        onMouseLeave={() => setActiveDropdown(null)}
                                    >
                                        <button className={`flex items-center gap-1 px-4 py-1.5 text-[12px] font-bold transition-all rounded-full tracking-wide ${activeDropdown === item.title ? 'text-bid-green bg-white/5' : 'text-white/80 hover:text-white hover:bg-white/5'}`}>
                                            {item.title}
                                            <ChevronDown size={12} className={`opacity-40 transition-transform duration-300 ${activeDropdown === item.title ? 'rotate-180' : ''}`} />
                                        </button>

                                        <AnimatePresence>
                                            {activeDropdown === item.title && (
                                                <motion.div
                                                    // @ts-ignore
                                                    initial={{ opacity: 0, y: 10 }}
                                                    animate={{ opacity: 1, y: 0 }}
                                                    exit={{ opacity: 0, y: 5 }}
                                                    transition={{ duration: 0.15 }}
                                                    className={`absolute top-full mt-2 bg-[#0A0F1C] border border-white/10 rounded-2xl shadow-[0_30px_60px_rgba(0,0,0,0.8)] z-[110] ${item.title === 'IT Services' ? 'w-[920px] right-[-180px] overflow-hidden' : 'w-72 bg-[#1e293b] border-white/10 left-1/2 -translate-x-1/2'}`}
                                                >
                                                    {item.title === 'IT Services' ? (
                                                        <div className="grid grid-cols-4 gap-8 p-10 bg-[#0A0F1C]">
                                                            {item.columns?.map((column, idx) => (
                                                                <div key={idx} className="space-y-6">
                                                                    <div className="flex flex-col">
                                                                        <h4 className="text-[13px] font-black text-white uppercase tracking-wider mb-2">{column.title}</h4>
                                                                        <div className="h-0.5 w-12 bg-emerald-500/40" />
                                                                    </div>
                                                                    <div className="flex flex-col gap-3">
                                                                        {column.links.map((link) => (
                                                                            <Link
                                                                                key={link.label}
                                                                                href={link.href}
                                                                                className="text-[12px] font-bold text-white/50 hover:text-emerald-400 transition-colors"
                                                                            >
                                                                                {link.label}
                                                                            </Link>
                                                                        ))}
                                                                    </div>
                                                                </div>
                                                            ))}
                                                        </div>
                                                    ) : (
                                                        <div className="px-1 py-3 bg-[#1e293b]">
                                                            {item.links?.map((link, idx) => (
                                                                <div key={idx} className="relative group/link">
                                                                    {link.isDivider ? (
                                                                        <div className="h-px bg-white/5 my-2 mx-4" />
                                                                    ) : (
                                                                        <>
                                                                            <Link
                                                                                href={link.href}
                                                                                className={`flex items-center justify-between px-5 py-2 text-[11px] font-bold transition-all rounded-xl mx-2 ${link.highlight ? 'text-bid-green bg-bid-green/5' : 'text-white/70 hover:text-bid-green hover:bg-white/5'
                                                                                    }`}
                                                                            >
                                                                                <div className="flex flex-col">
                                                                                    <span className="uppercase tracking-tight">{link.label}</span>
                                                                                    {link.price && <span className="text-[9px] font-black text-emerald-500/60 mt-0.5">{link.price}</span>}
                                                                                </div>
                                                                                {link.subLinks ? (
                                                                                    <ChevronRight size={13} className="opacity-40 group-hover/link:translate-x-1 transition-transform" />
                                                                                ) : link.hasArrow && (
                                                                                    <ChevronRight size={13} className="opacity-40" />
                                                                                )}
                                                                            </Link>

                                                                            {link.subLinks && (
                                                                                <div className="absolute top-0 left-full pl-4 hidden group-hover/link:block w-72">
                                                                                    <div className="bg-[#1e293b] border border-white/10 rounded-2xl shadow-[0_30px_60px_rgba(0,0,0,0.8)] py-3 max-h-[70vh] overflow-y-auto scrollbar-hide">
                                                                                        {link.subLinks.map((sub, sIdx) => (
                                                                                            <Link
                                                                                                key={sIdx}
                                                                                                href={sub.href}
                                                                                                className={`flex items-center justify-between px-6 py-2 text-[10px] font-bold transition-all ${sub.highlight ? 'text-bid-green' : 'text-white/60 hover:text-bid-green hover:bg-white/5'
                                                                                                    }`}
                                                                                            >
                                                                                                <span className="uppercase tracking-tight">{sub.label}</span>
                                                                                            </Link>
                                                                                        ))}
                                                                                    </div>
                                                                                </div>
                                                                            )}
                                                                        </>
                                                                    )}
                                                                </div>
                                                            ))}
                                                        </div>
                                                    )}
                                                </motion.div>
                                            )}
                                        </AnimatePresence>
                                    </div>
                                ))}

                                <Link href="/upload" className="flex items-center gap-1.5 px-4 py-1.5 text-[12px] font-black text-bid-green hover:text-white transition-all rounded-full bg-bid-green/5 border border-bid-green/10 hover:bg-bid-green/10 group ml-3 active:scale-95">
                                    <Sparkles size={13} className="transition-transform group-hover:scale-110" />
                                    Bid Analyser
                                </Link>
                            </nav>
                        </div>
                    </div>
                </div>

                {/* Mobile Menu */}
                <AnimatePresence>
                    {mobileMenuOpen && (
                        <motion.div
                            // @ts-ignore
                            initial={{ x: '100%' }}
                            animate={{ x: 0 }}
                            exit={{ x: '100%' }}
                            transition={{ type: 'spring', damping: 25, stiffness: 200 }}
                            className="lg:hidden fixed inset-0 z-[120] bg-bid-dark p-8 flex flex-col pointer-events-auto"
                        >
                            <div className="flex justify-between items-center mb-10">
                                <Image src="/bidalert_logo.png" alt="Logo" width={140} height={40} className="object-contain" />
                                <button onClick={() => setMobileMenuOpen(false)} className="p-2 text-white/60">
                                    <X size={28} />
                                </button>
                            </div>
                            <div className="flex flex-col gap-6 overflow-y-auto pr-2">
                                <Link href="/" onClick={() => setMobileMenuOpen(false)} className="text-lg font-bold text-white/80 hover:text-bid-green transition-colors">
                                    Home
                                </Link>
                                {navItems.map(item => (
                                    <div key={item.title} className="space-y-4">
                                        <h4 className="text-[11px] font-black uppercase tracking-[0.2em] text-white/20">{item.title}</h4>
                                        <div className="flex flex-col gap-4 pl-1">
                                            {item.title === 'IT Services' ? (
                                                item.columns?.map(column => (
                                                    <div key={column.title} className="space-y-2">
                                                        <h5 className="text-[9px] font-black text-white/40 uppercase tracking-widest">{column.title}</h5>
                                                        {column.links.map(sub => (
                                                            <Link key={sub.label} href={sub.href} onClick={() => setMobileMenuOpen(false)} className="text-sm font-bold text-white/80 block py-1">{sub.label}</Link>
                                                        ))}
                                                    </div>
                                                ))
                                            ) : (
                                                item.links?.map((sub, sIdx) => (
                                                    <div key={sIdx}>
                                                        {sub.isDivider ? (
                                                            <div className="h-px bg-white/10 my-4" />
                                                        ) : (
                                                            <>
                                                                <Link href={sub.href} onClick={() => !sub.subLinks && setMobileMenuOpen(false)} className={`text-lg font-bold flex items-center justify-between ${sub.highlight ? 'text-bid-green' : 'text-white/80'}`}>
                                                                    <div className="flex flex-col">
                                                                        <span className="uppercase tracking-tight">{sub.label}</span>
                                                                        {sub.price && <span className="text-[10px] font-black text-emerald-500/60 mt-1">{sub.price}</span>}
                                                                    </div>
                                                                    {sub.subLinks && <ChevronDown size={18} className="opacity-40" />}
                                                                </Link>
                                                                {sub.subLinks && (
                                                                    <div className="mt-4 flex flex-col gap-3 pl-4 border-l border-white/10">
                                                                        {sub.subLinks.map((nested, nIdx) => (
                                                                            <Link key={nIdx} href={nested.href} onClick={() => setMobileMenuOpen(false)} className="text-sm font-bold text-white/40 hover:text-bid-green uppercase tracking-tight">{nested.label}</Link>
                                                                        ))}
                                                                    </div>
                                                                )}
                                                            </>
                                                        )}
                                                    </div>
                                                ))
                                            )}
                                        </div>
                                    </div>
                                ))}
                            </div>
                            <div className="mt-auto pt-8 border-t border-white/5 flex flex-col gap-4">
                                {!isAuthenticated ? (
                                    <>
                                        <button
                                            onClick={() => { setMobileMenuOpen(false); openRegister(); }}
                                            className="w-full py-5 text-center font-black bg-bid-green text-bid-dark rounded-2xl shadow-xl uppercase tracking-widest"
                                        >
                                            GET STARTED
                                        </button>
                                        <div className="flex flex-col gap-2">
                                            <button onClick={() => { setMobileMenuOpen(false); openLogin(); }} className="w-full py-3 text-center font-bold text-white/80 uppercase tracking-widest text-xs">Admin Login</button>
                                            <button onClick={() => { setMobileMenuOpen(false); openLogin(); }} className="w-full py-3 text-center font-bold text-white/80 uppercase tracking-widest text-xs">My Requests</button>
                                        </div>
                                    </>
                                ) : (
                                    <div className="flex flex-col gap-3 w-full">
                                        <Link
                                            href="/my-requests"
                                            onClick={() => setMobileMenuOpen(false)}
                                            className="w-full py-5 text-center font-black bg-white/5 text-white/80 rounded-2xl border border-white/10 uppercase tracking-widest"
                                        >
                                            My Requests
                                        </Link>
                                        {user?.role === 'admin' && (
                                            <Link
                                                href="/admin/dashboard"
                                                onClick={() => setMobileMenuOpen(false)}
                                                className="w-full py-5 text-center font-black bg-bid-green/10 text-bid-green rounded-2xl border border-bid-green/20 uppercase tracking-widest"
                                            >
                                                Admin Panel
                                            </Link>
                                        )}
                                        <button onClick={() => { setMobileMenuOpen(false); logout(); }} className="w-full py-5 bg-red-500/10 text-red-400 font-bold rounded-2xl uppercase tracking-widest text-xs">Logout</button>
                                    </div>
                                )}
                            </div>
                        </motion.div>
                    )}
                </AnimatePresence>
            </header>

            {/* The spacer matches the height of the non-scrolled header */}
            <div className="h-[105px] bg-[#F1F7FF] transition-colors duration-500" />
        </>
    );
}
